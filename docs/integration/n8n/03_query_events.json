{
  "name": "3. 查询机台事件时间线 (Oracle直连)",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "events",
        "responseMode": "responseNode",
        "options": {}
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [240, 300],
      "webhookId": "events-webhook"
    },
    {
      "parameters": {
        "jsCode": "const body = $input.first().json.body || $input.first().json;\nlet machineId = (body.machine_id || '').trim();\nlet limit = parseInt(body.limit || 50);\nif (isNaN(limit) || limit < 1) limit = 50;\nif (limit > 500) limit = 500;\nmachineId = machineId.replace(/[^A-Za-z0-9_-]/g, '');\nreturn { json: { machine_id: machineId, limit: limit } };"
      },
      "name": "Parse",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [460, 300]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT event_ts_utc AS event_time, machine_id, event_name, event_value FROM DT_EVENT_RAW WHERE machine_id = '{{ $json.machine_id }}' AND parse_status = 'PARSED' ORDER BY event_ts_utc DESC FETCH FIRST {{ $json.limit }} ROWS ONLY",
        "options": {}
      },
      "name": "Query Oracle",
      "type": "n8n-nodes-base.oracle",
      "typeVersion": 1,
      "position": [680, 300],
      "credentials": {
        "oracleApi": { "id": "", "name": "FabTwin Oracle" }
      }
    },
    {
      "parameters": {
        "jsCode": "const rows = $input.first().json || [];\nconst arr = Array.isArray(rows) ? rows : [rows];\nconst count = arr.length;\nlet text = '';\nif (count === 0) {\n  text = '未找到事件记录';\n} else {\n  text = arr.map((r, i) => `${i+1}. ${r.event_time || ''} ${r.event_name || ''} ${r.event_value || ''}`).join('\\n');\n}\nreturn { json: { events: arr, count: count, summary: text } };"
      },
      "name": "Format",
      "type": "n8n-nodes-base.code",
      "typeVersion": 1,
      "position": [900, 300]
    },
    {
      "parameters": {
        "responseCode": 200,
        "responseBody": "={{ JSON.stringify($json) }}"
      },
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1120, 300]
    }
  ],
  "connections": {
    "Webhook": { "main": [[{ "node": "Parse", "type": "main", "index": 0 }]] },
    "Parse": { "main": [[{ "node": "Query Oracle", "type": "main", "index": 0 }]] },
    "Query Oracle": { "main": [[{ "node": "Format", "type": "main", "index": 0 }]] },
    "Format": { "main": [[{ "node": "Respond", "type": "main", "index": 0 }]] }
  }
}
