# FabTwin Pro 统一架构规划 v2.0

> 半导体工厂数字孪生可视化配置平台
> 更新日期：2026-07-14
> 当前进度：✅ VPO/OXE机台Vue化完成，视图切换正常

---

## 一、项目定位与边界

### 1.1 平台定位
**数字孪生可视化配置平台** — 专注于机台2D/3D模型的可视化呈现、动作匹配配置、历史回放与实时演示。

### 1.2 系统边界
| 职责 | 负责方 | 说明 |
|------|--------|------|
| RV资料接收与处理 | EAP/前置系统 | 不在本平台范围内 |
| 数据持久化（历史/实时） | Oracle/DB | 本平台只读消费 |
| 2D/3D模型制作 | 建模工具/设计 | 本平台提供配置与接入规范 |
| 动作匹配配置 | 本平台 | 事件 → 部件动作的可视化配置 |
| 可视化展示 | 本平台 | 机台详情、事件回放、数据面板 |
| AI对话/MCP | 本平台 | Lot追溯、告警分析等 |

### 1.3 数据流
```
EAP/RV → 标准化事件 → DB ← 本平台（只读）
                          ↓
                    实时WebSocket推送
                          ↓
                    前端可视化渲染
```

---

## 二、现有资产盘点

### 2.1 平台已有模块（test1分支）

| 模块 | 位置 | 状态 | 说明 |
|------|------|------|------|
| 后端API | `backend/` | ✅ 可用 | FastAPI + SQLAlchemy，10个路由模块 |
| 前端框架 | `frontend/` | ✅ 可用 | Vue 3 + Vite + Pinia + Element Plus |
| Dashboard看板 | `views/Dashboard.vue` | ✅ 可用 | KPI卡片、机台列表、告警统计 |
| 机台详情页 | `views/MachineDetail.vue` | ✅ 可用 | 3D/2D/VPO/OXE视图切换、事件/告警/Lot面板 |
| 3D模型编辑器 | `views/ModelEditor.vue` | ✅ 可用 | Three.js可视化配置，事件动作匹配 |
| 楼层平面图 | `components/FloorPlan.vue` | ✅ 可用 | 2D厂区布局 |
| AI对话助手 | `components/AiAssistant.vue` | ✅ 可用 | MCP接入框架 |
| WebSocket实时推送 | `services/realtime.py` | ✅ 可用 | 事件实时推送 |
| 数据模拟器 | `services/simulator.py` | ✅ 可用 | 模拟EAP事件生成 |
| 等角投影工具 | `composables/useIsoProjection.js` | ✅ 可用 | 自研2.5D等角投影 |
| 事件动作映射 | `composables/useEventActionMapping.js` | ✅ 可用 | 事件→动作映射系统 |

### 2.2 机台视图组件（Vue化完成）

| 组件 | 核心能力 | 渲染技术 | 事件系统 | 状态 |
|------|---------|---------|---------|------|
| `MachineIsoView.vue` | OXE机台2.5D等角视图、机械臂动画、Wafer流转、Pod放置、Mapping扫描 | Canvas 2D 自研等角投影 | 半导体专用事件链（POD_PLACED→LOCK→MVIN→DOOR_OPEN→LOAD→WaferLoaded→...） | ✅ Vue化完成 |
| `MachineVpoView.vue` | VPO机台2D视图、Pod穿入脱出动画、事件回放时间轴 | SVG矢量图 | POD_ATTACH/DETACH、STATE_CHANGE、ALARM | ✅ Vue化完成 |
| `MachineVpo3DView.vue` | VPO机台3D视图、Pod穿脱动画、伪3D投影 | Canvas 2D | POD_ATTACH/DETACH、STATE_CHANGE | ✅ Vue化完成 |
| `MachineModel3D.vue` | TEL DRM UNITY精细3D模型、7步工艺动画 | Three.js WebGL | STATE/SENSOR/ALARM/TRANSFER事件 | ✅ 可用 |

### 2.3 独立HTML资产（参考源）

| 文件 | 核心能力 | 渲染技术 | 用途 |
|------|---------|---------|------|
| `OXE_2D.html` | OXE机台2.5D等角视图、机械臂动画 | Canvas 2D | MachineIsoView.vue参考源 |
| `VPO2D.html` | VPO机台2D视图、Pod动画 | SVG + Canvas | MachineVpoView.vue参考源 |
| `VPO_3D.HTML` | VPO机台3D视图、Pod动画 | Canvas 2D | MachineVpo3DView.vue参考源 |

### 2.4 可复用资产分析

| OXE_2D 模块 | 可复用性 | 改造难度 | 现状 |
|-------------|---------|---------|------|
| 等角投影坐标系 | 高 | 中 | ✅ 已提取为 `useIsoProjection` 工具类 |
| 机械臂动画系统 | 中 | 中 | ✅ 已封装到 MachineIsoView.vue |
| Wafer流转逻辑 | 高 | 低 | ✅ 已集成到 MachineIsoView.vue |
| 事件状态机 | 中 | 高 | ✅ 已改造为事件-动作映射配置驱动 |
| Pod放置/门开关 | 高 | 低 | ✅ 已通用化 |

| VPO_2D/VPO_3D 模块 | 可复用性 | 改造难度 | 现状 |
|-------------------|---------|---------|------|
| 2D/3D双视图联动 | 高 | 中 | ✅ MachineVpoView + MachineVpo3DView |
| 热点高亮系统 | 高 | 低 | ✅ 已通用化 |
| POD穿入脱出动画 | 中 | 中 | ✅ 已封装到VPO组件 |
| 事件时间轴回放 | 高 | 低 | ✅ 已提取为HistoryReplay组件 |
| SVG部件绑定 | 中 | 中 | ✅ 已配置化 |

---

## 三、统一架构设计

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层 (Vue 3)                        │
├──────────────┬──────────────┬──────────────┬───────────────────┤
│  Dashboard   │ MachineDetail │  ModelEditor │  AI Assistant     │
│  总览看板     │  机台详情     │  模型配置器   │  智能对话         │
├──────────────┴──────────────┴──────────────┴───────────────────┤
│                    可视化渲染引擎层                               │
├─────────────────┬─────────────────┬────────────────────────────┤
│  3D渲染引擎      │  2D渲染引擎      │   动作播放引擎              │
│  (Three.js)     │  (Canvas/SVG)   │   (事件→动作映射)           │
├─────────────────┴─────────────────┴────────────────────────────┤
│                    数据与状态层 (Pinia)                           │
├──────────────┬──────────────┬──────────────┬───────────────────┤
│  机台模型Store │  事件Store    │  Lot Store    │  实时数据Store    │
└──────────────┴──────────────┴──────────────┴───────────────────┘
                              ↕ API / WebSocket
┌─────────────────────────────────────────────────────────────────┐
│                        后端服务层 (FastAPI)                        │
├──────────────┬──────────────┬──────────────┬───────────────────┤
│  机台管理API  │  事件API      │  Lot API     │   告警API          │
├──────────────┼──────────────┼──────────────┼───────────────────┤
│  模型配置API  │  动作匹配API  │  楼层/布局API │   AI/MCP API       │
├──────────────┴──────────────┴──────────────┴───────────────────┤
│                    数据访问层 (SQLAlchemy)                        │
└─────────────────────────────────────────────────────────────────┘
                              ↕ 只读
┌─────────────────────────────────────────────────────────────────┐
│                        数据层 (Oracle/DB)                         │
│  DT_EVENT_RAW / DT_EVENT_STD / DT_STATE_SNAPSHOT / DT_ALARM     │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 核心设计原则

1. **配置驱动，而非代码驱动**：新机台型号接入无需改代码，通过配置完成
2. **渲染与数据分离**：可视化引擎不直接依赖数据源，通过统一数据模型对接
3. **2D/3D统一规范**：同一套部件ID、同一套事件动作映射，2D/3D双视图
4. **渐进式接入**：从单机型深度定制 → 多机型配置化 → 全平台标准化

---

## 四、机台建模规范（核心）

### 4.1 机台模型定义 (MachineModel)

```json
{
  "model_id": "OXE-200",
  "model_name": "OXE 刻蚀机",
  "vendor": "Applied Materials",
  "process_type": "ETCH",
  "version": "1.0",
  
  "views": {
    "view_3d": {
      "type": "threejs",
      "model_url": "/models/machines/oxe-200.glb",
      "default_camera": { "position": [7, 5, 8], "target": [0, 1.8, 0] }
    },
    "view_2d": {
      "type": "isometric",
      "canvas_config": { "width": 800, "height": 600 },
      "projection": { "scale": 30, "angle_x": 30, "angle_y": 45 }
    }
  },
  
  "parts": [ /* 见 4.2 部件定义 */ ],
  "event_actions": [ /* 见 4.3 事件动作映射 */ ],
  "state_mapping": [ /* 见 4.4 状态映射 */ ]
}
```

### 4.2 部件定义 (Part)

每个部件在2D和3D视图中共享同一个 `part_id`，但有各自的渲染配置。

```json
{
  "part_id": "loadport_1",
  "part_name": "Load Port 1",
  "part_type": "loadport",
  "category": "material_handling",
  
  "default_state": {
    "visible": true,
    "opacity": 1.0,
    "color": "#2a3a5a"
  },
  
  "view_3d": {
    "mesh_name": "LP1_Mesh",
    "position": [-2.85, 1.8, -0.8],
    "rotation": [0, 0, 0],
    "scale": [1, 1, 1],
    "pivot_point": [0, 0, 0]
  },
  
  "view_2d_iso": {
    "x": 100,
    "y": 50,
    "z_order": 10,
    "shape": "rectangle",
    "width": 60,
    "height": 40,
    "isometric_depth": 20
  },
  
  "view_2d_svg": {
    "element_id": "lp1",
    "svg_path": "/views/vpo-2d.svg"
  },
  
  "hotspots": [
    {
      "hotspot_id": "lp1_door",
      "name": "Load Port门",
      "position_3d": [-2.85, 1.5, -0.8],
      "position_2d": { "x": 120, "y": 60 },
      "on_click": { "action": "show_info", "data": { "panel": "loadport_status" } }
    }
  ]
}
```

### 4.3 事件动作映射 (EventAction)

核心：**半导体事件 → 部件动作序列** 的配置化映射。

```json
{
  "mapping_id": "oxe_wafer_load_flow",
  "description": "晶圆装载完整流程",
  
  "trigger": {
    "event_type": "TRANSFER",
    "event_code": "WaferLoadStart",
    "match_condition": { "from": "loadport", "to": "chamber" }
  },
  
  "action_sequence": [
    {
      "step": 1,
      "part_id": "loadport_1_door",
      "action": "rotate",
      "params": { "axis": "y", "angle": 90, "duration": 1.0, "easing": "easeInOut" },
      "label": "打开Load Port门"
    },
    {
      "step": 2,
      "part_id": "efem_robot",
      "action": "move_along_path",
      "params": {
        "path": [
          { "pos": [-2, 1.5, -0.8], "time": 0 },
          { "pos": [-2.5, 1.5, -0.8], "time": 0.5 },
          { "pos": [-2.5, 1.5, -0.8], "time": 1.0 },
          { "pos": [-2, 1.5, -0.8], "time": 1.5 }
        ],
        "wafer_attached": true
      },
      "label": "EFEM机械臂取片"
    },
    {
      "step": 3,
      "part_id": "loadport_1_door",
      "action": "rotate",
      "params": { "axis": "y", "angle": -90, "duration": 1.0, "easing": "easeInOut" },
      "wait_for_step": 2,
      "label": "关闭Load Port门"
    }
  ],
  
  "rollback_on": {
    "event_type": "ALARM",
    "event_code": "TRANSFER_ABORT"
  }
}
```

### 4.4 状态映射 (StateMapping)

```json
{
  "state_id": "run",
  "state_name": "运行中",
  "color": "#10b981",
  "icon": "▶",
  
  "part_overrides": [
    { "part_id": "chamber_1", "emissive": "#10b981", "emissive_intensity": 0.5 },
    { "part_id": "status_light", "color": "#10b981", "pulse": true }
  ]
}
```

### 4.5 视图类型规范

| 视图类型 | 渲染技术 | 适用场景 | 配置文件格式 | 对应组件 |
|---------|---------|---------|------------|---------|
| `threejs` | Three.js WebGL | 3D全景展示、复杂模型 | GLB/GLTF + JSON配置 | MachineModel3D.vue |
| `isometric` | Canvas 2D等角投影 | 2.5D机台内部结构、工艺演示 | JSON部件定义 + Canvas绘制 | MachineIsoView.vue |
| `svg` | SVG矢量图 | 2D平面图、流程图、P&ID图 | SVG文件 + 热点配置 | MachineModel2D.vue |
| `vpo` | SVG矢量图 + Canvas叠加 | VPO氧化炉2D正视图、Pod穿脱动画 | SVG底图 + 动作配置 | MachineVpoView.vue |
| `vpo3d` | Canvas 2D伪3D投影 | VPO氧化炉3D视图、Pod穿脱动画 | JSON部件定义 + Canvas绘制 | MachineVpo3DView.vue |
| `hybrid_2d3d` | SVG + Canvas混合 | 2D/3D联动视图 | SVG底图 + 3D部件叠加 | - |

---

## 五、模型配置平台（ModelEditor 升级）

### 5.1 平台功能模块

```
ModelEditor
├── 机台型号管理
│   ├── 新建型号 / 编辑型号 / 删除型号
│   ├── 型号版本管理
│   └── 导入/导出配置 (JSON)
│
├── 3D模型编辑器
│   ├── 部件列表管理
│   ├── 3D视口中拖拽摆放
│   ├── 变换属性编辑（位置/旋转/缩放）
│   ├── 材质/颜色编辑
│   └── 部件层级关系
│
├── 2D视图编辑器
│   ├── 2D等角视图绘制
│   ├── SVG部件绑定
│   ├── 热点配置
│   └── 2D/3D部件ID映射
│
├── 事件动作配置器
│   ├── 事件触发器配置（事件类型/代码/条件）
│   ├── 动作序列编排（时间轴可视化）
│   ├── 动作类型库（移动/旋转/变色/显隐/路径动画）
│   ├── 缓动函数选择
│   └── 动作预览测试
│
├── 状态映射配置
│   ├── 机台状态定义
│   ├── 部件状态外观配置
│   └── 状态切换过渡动画
│
└── 数据绑定配置
    ├── 传感器数据 → 部件属性
    ├── Lot信息 → 晶圆显示
    └── 告警 → 高亮/闪烁
```

### 5.2 后端新增API

| API | 方法 | 说明 |
|-----|------|------|
| `/api/models` | GET | 获取机台型号列表 |
| `/api/models/{model_id}` | GET | 获取型号完整配置 |
| `/api/models` | POST | 新建机台型号 |
| `/api/models/{model_id}` | PUT | 更新型号配置 |
| `/api/models/{model_id}` | DELETE | 删除型号 |
| `/api/models/{model_id}/parts` | GET/PUT | 部件配置 |
| `/api/models/{model_id}/event-actions` | GET/PUT | 事件动作映射 |
| `/api/models/{model_id}/state-mapping` | GET/PUT | 状态映射 |
| `/api/models/import` | POST | 导入配置JSON |
| `/api/models/{model_id}/export` | GET | 导出配置JSON |

### 5.3 数据库新增表

```sql
-- 机台型号配置表
machine_models (
    model_id VARCHAR PK,
    model_name VARCHAR,
    vendor VARCHAR,
    process_type VARCHAR,
    version VARCHAR,
    config_json TEXT,       -- 完整配置JSON
    created_at VARCHAR,
    updated_at VARCHAR
)

-- 部件定义表（可从config_json中拆出，按需）
machine_parts (
    id INT PK AUTO,
    model_id VARCHAR FK,
    part_id VARCHAR,
    part_name VARCHAR,
    part_type VARCHAR,
    view_3d_json TEXT,
    view_2d_json TEXT,
    hotspots_json TEXT
)

-- 事件动作映射表
event_action_mappings (
    id INT PK AUTO,
    model_id VARCHAR FK,
    mapping_id VARCHAR,
    description VARCHAR,
    trigger_json TEXT,
    action_sequence_json TEXT,
    rollback_json TEXT
)
```

---

## 六、OXE / VPO Vue化改造路线

### 6.1 OXE_2D Vue化

#### 阶段一：组件拆分（1周）
```
src/components/oxe/
├── OxeViewer.vue           # 主组件（容器 + 生命周期）
├── OxeIsoCanvas.vue        # 等角投影Canvas
├── OxeRobotArm.vue         # 机械臂动画
├── OxeWaferFlow.vue        # 晶圆流转
├── OxeLoadPort.vue         # Load Port组件
├── OxeChamber.vue          # 腔体组件
└── composables/
    ├── useIsoProjection.js # 等角投影工具
    ├── useOxeState.js      # OXE状态管理
    └── useAnimationPlayer.js # 动作播放器
```

#### 阶段二：配置化改造（1周）
- 提取硬编码的部件位置/颜色到配置
- 事件状态机改为事件动作映射驱动
- 接入统一的 `MachineModel` 配置格式

#### 阶段三：集成到平台（3天）
- 接入 `MachineDetail` 的视图切换
- 对接 Pinia store 实时数据
- 事件回放联动

### 6.2 VPO_3D Vue化

#### 阶段一：组件拆分（1周）
```
src/components/vpo/
├── VpoViewer.vue           # 主组件
├── Vpo2DView.vue           # 2D SVG视图
├── Vpo3DView.vue           # 3D Canvas视图
├── VpoHotspotManager.vue   # 热点管理
├── VpoTimeline.vue         # 时间轴回放
└── composables/
    ├── useViewSync.js      # 2D/3D视图同步
    └── useVpoAnimation.js  # VPO动画系统
```

#### 阶段二：配置化改造（1周）
- SVG部件ID映射配置化
- 热点配置与动作解耦
- POD动画参数化

#### 阶段三：集成到平台（3天）
- 同OXE集成方式
- 时间轴与全局PlaybackBar联动

---

## 七、当前进度评估（结合分工图）

### 7.1 Sprint 1/2 目标回顾（2026/07/01 - 07/31）

| 责任人 | 目标 | 当前进度 | 完成度 | 状态 |
|--------|------|---------|-------|------|
| 李世庆 | VPO的2D接入平台，对应EAP上线 | ✅ VPO2D/VPO3D已Vue化，接入MachineDetail视图切换 | ✅ 100% | 已完成 |
| 钊钊 | DRM 2D图形接入（必达），EAP收值（挑战） | ✅ OXE等角视图已Vue化，DRM通用3D模型可用 | ✅ 100% | 已完成 |
| 佳琪 | Lot History的MCP与提示词接入 & Lot追溯的AI对话 | ✅ AI框架可用，Lot查询功能已实现 | ✅ 80% | 接近完成 |
| 亮亮 | 提供Lot History的MCP功能与接口 | ✅ 后端API完整，ai_mcp.py可用 | ✅ 80% | 接近完成 |

### 7.2 平台基础模块进度

| 模块 | 分工图对应 | 当前进度 | 说明 |
|------|-----------|---------|------|
| HomePage / 平台设计 | AI/平台 → HomePage → 平台设计 | ✅ 80% | Dashboard已可用，添加机台待完善 |
| 添加机台 | AI/平台 → HomePage → 添加机台 | ⚠️ 30% | 有机台管理API，UI未做 |
| 机台Page 基本功能 | AI/平台 → 机台Page → 基本功能 | ✅ 90% | 材料资讯/机台资讯面板已有，支持多视图切换 |
| 界面显示（半透明画布） | 材料资讯 → 界面显示 → 半透明画布显示 | ✅ 70% | 3D/2D/VPO/OXE画布均有，覆盖效果待优化 |
| 事件动作匹配 | EAP联动 → Flow → 2D图形链接 → 平台接入 | ✅ 70% | ModelEditor有雏形，VPO/OXE事件动作映射已配置化 |
| AI对话查询 Lot History | 附加功能 → AI对话查询 → Lot History → MCP | ✅ 70% | AI组件有，Lot查询已对接 |
| Lot追溯 | 附加功能 → Lot追溯 → By LOT ID → AI对话查询 → MCP | ✅ 60% | 框架完整，UI待完善 |
| Alarm追溯 | 附加功能 → Alarm追溯 → 跳转ByTime | ✅ 60% | 时间轴有，告警查询已实现 |
| 3D建模研究 | 3D建模 → 研究 | ✅ 70% | Three.js基础可用，配置编辑器可用 |

### 7.3 关键差距分析

| 差距 | 影响 | 优先级 | 状态 |
|------|------|-------|------|
| 机台型号配置体系缺失 | 新机台接入需要改代码 | 🟡 中 | ✅ 已建立基础，需完善 |
| 事件-动作配置化未落地 | 每种机台事件需要硬编码 | 🟡 中 | ✅ VPO/OXE已配置化 |
| OXE/VPO未Vue组件化 | 无法集成到机台详情页 | ✅ 已完成 | ✅ 已完成 |
| 2D/3D统一规范缺失 | 2D和3D各做各的，无法复用 | 🟡 中 | ⚠️ 进行中 |
| MCP/Lot History接口不完善 | AI对话功能受限 | 🟡 中 | ⚠️ 进行中 |
| 时间轴回放与动作播放引擎未统一 | 回放功能碎片化 | 🟡 中 | ⚠️ 进行中 |

---

## 八、后续开发计划（建议）

### Sprint 2（07/17 - 07/31）：架构统一 + VPO接入

**目标**：建立统一建模规范，完成VPO Vue化接入，跑通配置化流程

| 任务 | 负责人建议 | 工时 | 交付物 |
|------|-----------|------|-------|
| 机台型号配置体系（后端+DB） | 后端 | 3天 | MachineModel API + 数据表 |
| 事件动作映射配置（后端） | 后端 | 2天 | EventAction API |
| VPO_3D Vue组件化 | 李世庆 | 5天 | VpoViewer.vue 系列组件 |
| VPO接入MachineDetail + 配置化 | 李世庆 + 前端 | 3天 | VPO视图可配置化切换 |
| ModelEditor升级 - 型号管理 | 前端 | 3天 | 型号列表/新建/导入导出 |
| ModelEditor升级 - 事件动作配置器 | 前端 | 4天 | 可视化动作编排UI |
| Lot History MCP接口完善 | 亮亮 | 3天 | 完整Lot History查询API |
| AI对话接入Lot History | 佳琪 | 3天 | AI可查询Lot历史 |

**Sprint 2 结束验收标准**：
- ✅ VPO机台可通过配置接入平台（无需改代码）
- ✅ ModelEditor可配置事件动作映射并预览
- ✅ AI助手可查询Lot History数据

---

### Sprint 3（08/01 - 08/14）：OXE接入 + 2D规范完善

**目标**：完成OXE Vue化接入，完善2D等角投影规范，DRM接入

| 任务 | 负责人建议 | 工时 | 交付物 |
|------|-----------|------|-------|
| OXE_2D Vue组件化 | 钊钊 | 5天 | OxeViewer.vue 系列组件 |
| 等角投影工具库（IsoProjection） | 钊钊 | 2天 | 通用2.5D渲染工具 |
| 晶圆传输动画组件（通用） | 钊钊 | 2天 | 可配置的Wafer Flow组件 |
| OXE配置化接入 + 事件匹配 | 钊钊 + 前端 | 3天 | OXE机台可配置接入 |
| DRM机台模型配置 | 钊钊 | 3天 | DRM 2D图形配置 |
| 动作播放引擎（统一） | 前端 | 3天 | 通用AnimationPlayer |
| 时间轴回放完善 | 前端 | 2天 | 支持倍速/逐帧/跳转 |
| EAP收值对接（挑战目标） | 后端 | 3天 | 真实EAP数据接入验证 |

**Sprint 3 结束验收标准**：
- ✅ OXE机台可配置化接入
- ✅ DRM机台2D图形接入（必达目标）
- ✅ 统一动作播放引擎，2D/3D共用
- ✅ 事件回放时间轴功能完善

---

### Sprint 4（08/15 - 08/28）：Endura接入 + 平台化完善

**目标**：Endura接入，平台功能完善，AI能力增强

| 任务 | 负责人建议 | 工时 | 交付物 |
|------|-----------|------|-------|
| Endura Demo + Flow + 配置化 | 待定 | 5天 | Endura机台接入 |
| HomePage 添加机台功能 | 前端 | 2天 | 可视化添加机台UI |
| 楼层布局与机台联动完善 | 前端 | 2天 | 2D楼层+3D视图联动 |
| 告警History MCP + EQAS对接 | 亮亮 + 佳琪 | 4天 | 告警查询AI化 |
| Lot Handling MCP | 亮亮 + 佳琪 | 3天 | Run/Hold/Release等 |
| 性能优化（大数据量） | 全栈 | 3天 | 卡顿/内存优化 |
| 配置版本管理 | 后端 + 前端 | 2天 | 配置版本回滚/对比 |

**Sprint 4 结束验收标准**：
- ✅ Endura机台接入平台
- ✅ 三种机型（VPO/DRM/Endura）全部配置化接入
- ✅ AI助手覆盖Lot/Alarm/Handling三大场景
- ✅ 平台可稳定运行

---

## 九、风险与建议

### 9.1 主要风险

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| OXE/VPO HTML→Vue改造量大，Sprint 1未完成 | 高 | 进度延后 | 优先保证VPO（Sprint 1必达），OXE放Sprint 3 |
| 配置化程度不足，新机台仍需改代码 | 中 | 平台价值打折扣 | 先从VPO验证配置化可行性，再推广 |
| EAP真实数据接入不确定 | 中 | 演示效果受限 | 模拟器兜底，EAP数据接入作为挑战目标 |
| 2D/3D规范统一难度超预期 | 中 | 返工 | 先做2D和3D各一套，Sprint 3再考虑统一 |

### 9.2 关键建议

1. **先跑通一条线**：VPO → 配置化 → 平台接入，跑通完整链路再复制
2. **配置优于编码**：所有新功能先想"能不能配置化"，再考虑硬编码
3. **渐进式重构**：现有HTML先拆组件再配置化，不要一步到位
4. **重视编辑器**：ModelEditor是平台核心，投入要够，不然配置化是空谈
5. **数据驱动演示**：用模拟器生成真实感数据，比空架子有说服力

---

## 十、文件结构调整建议

```
frontend/src/
├── components/
│   ├── common/              # 通用组件
│   │   ├── PlaybackBar.vue
│   │   ├── EventTimeline.vue
│   │   └── Hotspot.vue
│   ├── machine/             # 机台可视化
│   │   ├── MachineViewer3D.vue      # 通用3D视图
│   │   ├── MachineViewer2D.vue      # 通用2D视图
│   │   ├── MachineViewerIso.vue     # 等角2.5D视图
│   │   └── AnimationPlayer.vue      # 动作播放器
│   ├── vpo/                 # VPO专用组件
│   │   ├── VpoViewer.vue
│   │   ├── Vpo2DView.vue
│   │   └── Vpo3DView.vue
│   └── oxe/                 # OXE专用组件
│       ├── OxeViewer.vue
│       └── OxeIsoCanvas.vue
├── composables/
│   ├── useMachineModel.js    # 机台模型加载
│   ├── useAnimationPlayer.js # 动作播放
│   ├── useIsoProjection.js   # 等角投影
│   └── useEventAction.js     # 事件动作映射
├── stores/
│   ├── app.js
│   ├── machineModel.js       # 机台模型配置
│   └── playback.js           # 回放控制
└── views/
    ├── Dashboard.vue
    ├── MachineDetail.vue
    └── ModelEditor.vue       # 升级为完整配置平台

backend/
├── routers/
│   ├── models.py             # 新增：机台型号配置API
│   └── ...
├── models.py                 # 新增：MachineModel等表
└── services/
    └── model_config.py       # 新增：模型配置服务
```
