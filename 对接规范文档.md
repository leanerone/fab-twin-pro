# FabTwin 与世庆对接规范文档

## 一、项目背景

本项目是半导体工厂数字孪生平台，世庆负责：
- 3D 模型创建与渲染
- TIBCO Rendezvous (RV) 消息接收与存入 Oracle DB

我方负责：
- 数字孪生平台后端（API + 缓存 + 数据分析）
- 前端界面（看板 + 机台详情 + 历史回放 + AI助手）

---

## 二、3D模型规范

### 2.1 模型格式要求

| 项目 | 要求 |
|------|------|
| 建模工具 | Blender 3.x |
| 导出格式 | GLB / GLTF 2.0 |
| 单位 | 米 (m) |
| 坐标系 | 右手坐标系 (Y轴向上) |
| 命名规范 | `machine-{型号}-{版本}.glb` |

### 2.2 机台模型结构

```
ETCH-TEL-DRM-UNITY-v1.glb
├── root
│   ├── base (基座)
│   ├── chambers (腔体组)
│   │   ├── chamber_1
│   │   ├── chamber_2
│   │   ├── chamber_3
│   │   └── chamber_4
│   ├── arm (机械臂)
│   └── smif (SMIF接口，仅Line-2有)
```

### 2.3 动画规范

| 动画名称 | 说明 | 触发条件 |
|----------|------|----------|
| idle | 待机状态 | state = idle |
| run | 运行状态 | state = run |
| open_chamber | 腔体打开 | 取放晶圆 |
| transfer | 晶圆传送 | 机械臂动作 |
| error | 告警状态 | state = error |

### 2.4 模型交付流程

1. 世庆完成模型制作和动画绑定
2. 导出为 GLB 格式，命名如 `ETCH-TEL-DRM-UNITY-v1.glb`
3. 放入 `frontend/public/models/` 目录
4. 前端通过 Three.js 加载渲染

---

## 三、数据接口规范

### 3.1 数据源：Oracle DB 表结构

世庆从 TIBCO RV 接收消息后，存入以下4张表：

#### DT_EVENT_RAW（原始事件表）
| 字段 | 类型 | 说明 |
|------|------|------|
| raw_id | VARCHAR2(64) | 主键 |
| tool_id | VARCHAR2(32) | 机台ID |
| source_system | VARCHAR2(32) | 来源系统 |
| source_message_id | VARCHAR2(64) | 原始消息ID |
| received_ts_utc | VARCHAR2(32) | 接收时间 |
| event_ts_utc | VARCHAR2(32) | 事件时间 |
| payload_json | CLOB | 原始JSON |
| parse_status | VARCHAR2(16) | 解析状态 |

#### DT_EVENT_STD（标准化事件表）
| 字段 | 类型 | 说明 |
|------|------|------|
| std_id | VARCHAR2(64) | 主键 |
| raw_id | VARCHAR2(64) | 关联原始事件 |
| tool_id | VARCHAR2(32) | 机台ID |
| event_type | VARCHAR2(32) | 事件类型 |
| event_code | VARCHAR2(32) | 事件代码 |
| event_value | VARCHAR2(256) | 事件值 |
| event_ts_utc | VARCHAR2(32) | 事件时间 |

#### DT_STATE_SNAPSHOT（状态快照表）
| 字段 | 类型 | 说明 |
|------|------|------|
| snap_id | VARCHAR2(64) | 主键 |
| tool_id | VARCHAR2(32) | 机台ID |
| chamber_id | VARCHAR2(16) | 腔体ID |
| chamber_state | VARCHAR2(16) | 腔体状态 |
| wafer_id | VARCHAR2(32) | 晶圆ID |
| recipe_id | VARCHAR2(32) | 配方ID |
| step_id | VARCHAR2(16) | 步骤ID |
| snap_ts_utc | VARCHAR2(32) | 快照时间 |

#### DT_ALARM_EVENT（告警事件表）
| 字段 | 类型 | 说明 |
|------|------|------|
| alarm_id | VARCHAR2(64) | 主键 |
| tool_id | VARCHAR2(32) | 机台ID |
| alarm_code | VARCHAR2(32) | 告警代码 |
| alarm_level | VARCHAR2(16) | 告警级别 |
| alarm_desc | VARCHAR2(512) | 告警描述 |
| occurred_ts_utc | VARCHAR2(32) | 发生时间 |
| resolved_ts_utc | VARCHAR2(32) | 解决时间 |

### 3.2 我方对接方式

**方案：直接从 Oracle DB 读取**

- 世庆实时将 RV 消息存入 Oracle
- 我方通过 ODS 层定期同步（或 CDC 实时同步）
- 优点：与世庆解耦，不依赖 RV 客户端
- 响应延迟：取决于同步频率（建议 1-5秒）

**连接配置**（修改 `backend/config.py`）：

```python
# Oracle 生产模式配置
ORACLE_URL = "oracle+cx_oracle://username:password@host:1521/service_name"
ORACLE_ENABLED = True  # 切换为 True 启用 Oracle
```

### 3.3 API 接口列表

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/machines` | GET | 获取所有机台列表 |
| `/api/machines/{id}` | GET | 获取机台详情 |
| `/api/machines/stats` | GET | 获取 KPI 统计 |
| `/api/events` | GET | 获取事件列表 |
| `/api/events/history` | GET | 历史回放查询 |
| `/api/lots` | GET | 获取 Lot 列表 |
| `/api/alarms` | GET | 获取告警列表 |
| `/api/ai/query` | POST | AI 自然语言查询 |
| `/ws/realtime` | WS | 实时事件推送 |

---

## 四、数据中台 + 缓存层设计

### 4.1 架构说明

```
┌─────────────────────────────────────────────────────────────┐
│                      用户前端 (Vue3)                        │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTP/WebSocket
┌─────────────────────────▼───────────────────────────────────┐
│                    后端服务 (FastAPI)                        │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │  API层    │ │ 业务逻辑  │ │  AI分析   │ │  WebSocket│   │
│  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘   │
└───────┬──────────────┼──────────────┼──────────────┼───────┘
        │              │              │              │
┌───────▼──────────────▼──────────────▼──────────────▼───────┐
│                    缓存层 (Redis)                           │
│  - 机台实时状态 (TTL 60s)                                   │
│  - 热点事件数据                                              │
│  - KPI 指标缓存                                              │
│  - WebSocket 消息队列                                        │
└───────┬─────────────────────────────────────────────────────┘
        │
┌───────▼─────────────────────────────────────────────────────┐
│                    ODS层 (SQLite/Oracle)                    │
│  - DT_EVENT_RAW / DT_EVENT_STD / DT_STATE_SNAPSHOT / ALARM │
│  - Machine / Lot / Recipe / OHT / AIInsight                 │
└───────┬─────────────────────────────────────────────────────┘
        │ 定期同步 (每5秒) / CDC实时
┌───────▼─────────────────────────────────────────────────────┐
│              源系统 (MES/RCMS/FDC/Oracle)                    │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 减少源系统负载的方法

**方案一：ODS 层同步（推荐）**
- 不在源系统上直接查询
- 通过 ODS 层定期从 Oracle 同步数据
- 同步频率可配置（默认每5秒）

**方案二：RV 消息订阅**
- 直接订阅 RV 消息队列
- 需要安装 TIBCO RV 客户端
- 优点：实时性更好，不查数据库

**方案三：混合模式**
- 实时事件走 RV 订阅
- 历史数据走 ODS 同步

### 4.3 Redis 缓存策略

| 缓存项 | Key | TTL | 更新时机 |
|--------|-----|-----|----------|
| 机台状态 | `machine:{id}:state` | 60s | 状态变更时 |
| 事件列表 | `machine:{id}:events` | 30s | 新事件到达 |
| KPI 统计 | `dashboard:kpi` | 120s | 定时刷新 |
| 用户会话 | `user:{token}` | 8h | 登录时 |

### 4.4 AI 接入方案（N8N vs Dify）

| 对比项 | N8N | Dify |
|--------|-----|------|
| 定位 | 工作流引擎 | AI 应用平台 |
| 优势 | 连接多个系统，编排复杂流程 | 专注 AI，支持 RAG，Prompt管理 |
| 适合场景 | 需要对接多个外部系统 | 需要快速构建 AI 问答应用 |

**推荐方案：Dify + N8N 组合**
- **Dify**：管理 AI 模型、Prompt、RAG 知识库
- **N8N**：编排业务流程，调用外部系统 API

---

## 五、协作流程

### 5.1 日常协作

1. **每日同步**：早上 10:00 站会，同步进度
2. **模型交付**：世庆完成模型后，提交到 `frontend/public/models/`
3. **接口联调**：世庆提供 Oracle 连接信息，我方配置 ODS 同步
4. **问题反馈**：使用飞书文档记录问题，标记优先级

### 5.2 联调步骤

#### 第一步：Oracle 连接测试
```
1. 世庆提供连接信息：host, port, service_name, username, password
2. 我方配置 backend/config.py 中的 ORACLE_URL
3. 测试连接：python -c "from database import test_oracle_connection"
```

#### 第二步：数据同步验证
```
1. 启动后端服务
2. 查看日志：确认 ODS 同步是否正常
3. 检查 API 返回：确认数据正确
```

#### 第三步：3D 模型集成
```
1. 世庆将模型放入 frontend/public/models/
2. 前端添加模型加载代码
3. 测试模型显示和动画播放
```

---

## 六、一键部署说明

### 6.1 环境要求

| 软件 | 版本 | 用途 |
|------|------|------|
| Python | 3.10+ | 后端运行 |
| Node.js | 16+ | 前端构建 |
| Redis | 6+ | 缓存层（可选） |

### 6.2 部署步骤

```bash
# 1. 进入项目目录
cd fab-twin-pro

# 2. 运行一键部署脚本
deploy.bat

# 3. 打开浏览器访问
# 前端：http://localhost:5173
# 后端API：http://localhost:8001
# API文档：http://localhost:8001/docs
```

### 6.3 内网环境注意事项

1. **依赖安装失败**：内网可能无法访问外网，需要提前下载依赖包
2. **Redis 不可用**：Redis 是可选组件，禁用后使用内存缓存
3. **Oracle 连接**：确保网络可达 Oracle 服务器

---

## 七、项目文件结构

```
fab-twin-pro/
├── backend/                    # 后端服务
│   ├── main.py                 # 入口文件
│   ├── config.py              # 配置文件
│   ├── models.py              # 数据库模型
│   ├── database.py            # 数据库连接
│   ├── seed_data.py           # 模拟数据生成
│   ├── routers/               # API路由
│   │   ├── machines.py        # 机台API
│   │   ├── events.py          # 事件API
│   │   ├── lots.py            # Lot API
│   │   ├── alarms.py          # 告警API
│   │   ├── ai.py              # AI API
│   │   └── oht.py             # OHT API
│   ├── services/              # 业务服务
│   │   ├── cache.py           # Redis缓存
│   │   ├── simulator.py       # 模拟器
│   │   ├── ods.py             # ODS同步
│   │   ├── ai_mcp.py          # AI调用框架
│   │   └── realtime.py        # WebSocket管理
│   └── requirements.txt       # Python依赖
├── frontend/                  # 前端应用
│   ├── src/
│   │   ├── components/        # Vue组件
│   │   ├── pages/             # 页面
│   │   ├── api/               # API调用
│   │   └── stores/            # 状态管理
│   ├── public/models/         # 3D模型目录
│   ├── package.json           # Node依赖
│   └── vite.config.js         # Vite配置
├── deploy.bat                 # 一键部署脚本
└── 对接规范文档.md            # 本文档
```

---

## 八、后续开发计划

| 阶段 | 内容 | 时间 |
|------|------|------|
| 第一阶段 | Demo 完成（当前） | 已完成 |
| 第二阶段 | 接入 Oracle 生产数据 | 下周 |
| 第三阶段 | 集成 3D 模型和动画 | 2周 |
| 第四阶段 | AI 功能上线 | 3周 |
| 第五阶段 | 历史回放功能完善 | 4周 |