# FabTwin Pro 数字孪生项目总结

## 一、项目概述

**项目定位**：半导体工厂数字孪生平台（Fab Digital Twin），用于监控 TEL DRM UNITY 刻蚀机等设备的实时状态、历史回放、楼层平面图编辑、OHT 天车调度可视化及 AI 辅助查询。

**技术栈**：
- 后端：Python 3.10+ / FastAPI / SQLAlchemy 2.0 / SQLite（Demo，可切 Oracle 19c）/ Redis（可选）
- 前端：Vue 3.4 + Vite 5 + Pinia 2 + Vue Router 4 + Three.js 0.160（3D 可视化）
- 通信：REST API + WebSocket 实时推送

---

## 二、架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                  用户浏览器                        │
│  Vue3 + Vite + Three.js + Pinia + Vue Router    │
│  端口: http://localhost:5173                     │
└──────────────┬──────────────────┬───────────────┘
           REST API            WebSocket
               │                    │
┌──────────────▼──────────────────▼───────────────┐
│              FastAPI 后端 (端口 8001)              │
│  ┌─────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ 8个路由  │ │ 模拟器   │ │ WebSocket管理器   │  │
│  │ 模块    │ │ (7步工艺)│ │ (实时广播)        │  │
│  └─────────┘ └──────────┘ └──────────────────┘  │
│  ┌─────────────────┐ ┌────────────────────────┐ │
│  │ Redis缓存(可选)  │ │ AI MCP框架(Dify/n8n)   │ │
│  │ 失败回退内存     │ │ 失败回退本地规则引擎    │ │
│  └─────────────────┘ └────────────────────────┘ │
└──────────────┬──────────────────────────────────┘
               │ SQLAlchemy ORM
┌──────────────▼──────────────────────────────────┐
│           SQLite / Oracle 19c                    │
│  4张现有表 + 12张扩展表 = 16张表                 │
└──────────────┬──────────────────────────────────┘
               │ ODS同步(预留)
┌──────────────▼──────────────────────────────────┐
│      TIBRV / MES / RCMS / FDC (源系统)           │
└─────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
fab-twin-pro/
├── backend/                    # FastAPI 后端
│   ├── main.py                # 入口(路由注册+WS+启动)
│   ├── config.py              # 配置中心(DB/Redis/AI/ODS)
│   ├── models.py              # 16张表 ORM
│   ├── schemas.py             # Pydantic 模型
│   ├── seed_data.py           # 种子数据生成
│   ├── requirements.txt       # Python 依赖
│   ├── routers/               # 8个API路由模块
│   │   ├── machines.py        # 机台+KPI统计
│   │   ├── events.py          # 事件+趋势时间轴
│   │   ├── lots.py            # 批次查询
│   │   ├── alarms.py          # 告警统计
│   │   ├── ai.py              # AI自然语言查询
│   │   ├── floors.py          # 楼层/区域/轨迹/天车CRUD
│   │   ├── oht.py             # 天车位置
│   │   └── recipes.py         # 工艺配方
│   └── services/              # 业务服务层
│       ├── cache.py           # Redis缓存(含内存回退)
│       ├── realtime.py        # WebSocket管理
│       ├── simulator.py       # 7步工艺模拟器
│       ├── ods.py             # ODS数据中台
│       └── ai_mcp.py          # AI MCP调用框架
├── frontend/                   # Vue3 前端
│   └── src/
│       ├── views/             # 2个页面
│       │   ├── Dashboard.vue  # 主页看板
│       │   └── MachineDetail.vue # 机台详情
│       ├── components/        # 12个组件
│       │   ├── FloorView3D.vue    # 楼层3D视图
│       │   ├── FloorPlan.vue      # 2D平面图编辑器
│       │   ├── MachineModel3D.vue # TEL DRM UNITY精细模型
│       │   ├── KpiCards.vue       # 7个KPI卡片
│       │   ├── AiAssistant.vue    # AI助手聊天
│       │   └── ...
│       ├── stores/app.js      # Pinia全局状态
│       └── api/index.js       # API统一封装
├── deploy.bat                 # 一键部署脚本
├── start-dev.bat              # 开发启动脚本
└── 对接规范文档.md             # 与世庆的协作规范
```

### 2.3 数据库设计

**现有 4 张表（与 Oracle 对齐）**：
| 表名 | 说明 |
|------|------|
| `dt_event_raw` | 原始事件表（RV 报文，TIBRV 来源） |
| `dt_event_std` | 标准化事件表 |
| `dt_state_snapshot` | 状态快照表 |
| `dt_alarm_event` | 告警事件表 |

**扩展 12 张表（数字孪生业务）**：
| 表名 | 说明 |
|------|------|
| `machines` | 机台主数据（含 floor_x/floor_y 平面坐标） |
| `lots` | 批次主数据 |
| `recipes` | 工艺配方 |
| `chamber_snapshots` | 腔体级状态快照（PM-1~PM-4） |
| `oht_positions` | OHT 天车位置历史 |
| `ai_insights` | AI 分析结果 |
| `machine_events` | 机台事件流（STATE/SENSOR/ALARM/TRANSFER） |
| `alarms` | 告警记录 |
| `dashboard_kpi` | 看板预计算 KPI |
| `floors` | 楼层信息（1F-4F） |
| `floor_areas` | 楼层区域（设备区/过道/电梯/逃生门等） |
| `tracks` | 天车轨迹（2D 绘制点 JSON） |
| `vehicles` | 天车/搬运车（绑定轨迹） |

---

## 三、已完成功能清单

### 3.1 主页看板
- ✅ 7个KPI卡片（运行/空闲/告警/产量/WIP/节拍/OEE）
- ✅ 楼层选择器（1F-4F），切换时3D模型同步切换
- ✅ 机台列表（Line1/Line2分类，Line1无SMIF，Line2有SMIF OHT）
- ✅ 3D/平面图视图切换

### 3.2 楼层3D视图
- ✅ 机台3D模型+状态灯实时颜色（绿色=运行/黄色=空闲/红色=故障/蓝色=维护/紫色=准备）
- ✅ 区域显示（设备区/过道/STK/PUMP/电梯/逃生门等）
- ✅ 天车沿轨迹循环移动动画
- ✅ 天车3D模型（车厢+POD+FOUP+Wafer）
- ✅ 点击机台跳转详情页

### 3.3 2D平面图编辑器
- ✅ 5种工具（选择拖拽/添加机台/画区域/画轨迹/添加天车）
- ✅ 拖拽调整机台和区域位置
- ✅ 画轨迹并保存，支持撤销/清空
- ✅ 添加天车并绑定轨迹（智能ID生成，避免冲突）
- ✅ 天车2D移动动画（requestAnimationFrame）
- ✅ 天车列表（侧边栏查看和删除）
- ✅ JSON导入导出楼层平面图

### 3.4 机台详情页
- ✅ TEL DRM UNITY精细3D模型（EFEM+VTM+4腔体+气体面板+三色塔灯+控制屏）
- ✅ 7步工艺动画（等待→装载→抽真空→刻蚀#1→刻蚀#2→充气→卸载）
- ✅ 实时/回放双模式切换
- ✅ 回放控制（日期选择+6档倍速0.5x-16x+时间轴拖拽）
- ✅ 右侧4Tab（告警统计/事件流/Lot批次/AI助手）
- ✅ Lot点击跳转回放时间

### 3.5 实时推送
- ✅ WebSocket连接+指数退避自动重连
- ✅ 机台状态/事件实时广播

### 3.6 数据层
- ✅ 16张表（4张Oracle对齐+12张扩展）
- ✅ 模拟数据生成（33台机台+Lot+事件+告警）
- ✅ Redis缓存（可选，内存回退）
- ✅ ODS数据中台框架

### 3.7 AI助手
- ✅ 自然语言查询（7类问题：机台状态/告警/温度/产量/异常/Lot/外部系统）
- ✅ SQL展示+回放跳转
- ✅ 外部MCP框架（Dify/n8n）预留接口

### 3.8 部署
- ✅ 一键部署脚本（deploy.bat）
- ✅ 开发启动脚本（start-dev.bat）

---

## 四、运行方式

### 4.1 开发模式

```bash
# 方式一：使用启动脚本
start-dev.bat

# 方式二：手动启动
# 后端（端口8001）
cd backend
python main.py

# 前端（端口5173）
cd frontend
npm run dev
```

### 4.2 访问地址
- 前端：http://localhost:5173（或5174，5173被占用时自动切换）
- 后端API：http://localhost:8001/api
- WebSocket：ws://localhost:8001/ws/realtime

### 4.3 一键部署
```bash
deploy.bat
```

---

## 五、关键设计决策

### 5.1 降级策略
- **Redis**：不可用时自动回退内存缓存，不影响系统运行
- **AI MCP**：未启用时回退本地规则引擎
- **数据库**：SQLite可无缝切换Oracle，仅需修改配置

### 5.2 数据流设计
```
前端(Vue3) → API(FastAPI) → 缓存(Redis,可选) → ODS(SQLite/Oracle) → 源系统(TIBRV/MES/RCMS/FDC)
```

### 5.3 实时性设计
- 模拟器后台任务每2-3秒推进所有机台工艺步骤
- 通过WebSocket广播STATE/SENSOR事件到前端
- 前端状态灯实时颜色过渡（Lerp动画）

### 5.4 3D可视化体系
- **产线总览**（LineView3D）：双产线俯瞰+SMIF OHT轨道
- **楼层视图**（FloorView3D）：按楼层加载机台/区域/轨迹/天车
- **单机精细模型**（MachineModel3D）：EFEM/VTM/4腔体/气体面板/控制屏动画

---

## 六、后续接入要点

### 6.1 Oracle连接（依赖世庆）
1. 获取连接信息：host、port、service_name、username、password
2. 安装Oracle驱动：`pip install oracledb`
3. 修改 `backend/config.py` 中的 `DATABASE_URL`

### 6.2 3D模型接入（依赖世庆）
1. Blender建模→导出GLB格式
2. 命名规范：`ETCH-TEL-DRM-UNITY-v1.glb`
3. 放置到 `frontend/public/models/`

### 6.3 AI升级
1. 部署Dify（对话管理+RAG）
2. 部署n8n（工具编排）
3. 配置 `backend/config.py` 中的AI MCP参数

---

## 七、已知问题与改进方向

### 7.1 当前限制
- 数据库使用SQLite，需切换Oracle
- AI功能使用本地规则引擎，需接入真实LLM
- 3D模型为代码生成，需替换为Blender导出的GLB

### 7.2 性能优化
- 3D模型LOD（多层次细节）
- API分页优化
- 前端虚拟滚动

### 7.3 功能扩展
- 用户权限管理
- 告警通知（邮件/钉钉/企业微信）
- 多机台扩展（1000+台）
- 历史数据归档
